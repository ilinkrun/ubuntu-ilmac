#------------------------------------------------------------------------------
# Platform Configuration
PLATFORM_NAME=${PLATFORM_NAME}
PLATFORM_DESCRIPTION="${PLATFORM_DESCRIPTION}"
PLATFORM_SN=${PLATFORM_SN}

#------------------------------------------------------------------------------
# 타임존 설정
TIMEZONE=Asia/Seoul

#------------------------------------------------------------------------------
# # IP
# ## Ilmac
# IP_ILMAC=
# URL_ILMAC=
# ## Home
# IP_HOME=

#------------------------------------------------------------------------------
# DATABASE

## External Database Connections (user configured)
# Mysql
MYSQL_HOST=1.231.118.217
MYSQL_PORT=2306
MYSQL_USER=root
MYSQL_PASSWORD=mysqlIlmac1!

# PostgreSQL
POSTGRES_HOST=1.231.118.217
POSTGRES_USER=admin
POSTGRES_PASSWORD="IlmacPost9)"
POSTGRES_PORT=5433

# Security
JWT_SECRET_SALT=${JWT_SECRET_SALT}
ENCRYPTION_KEY=${ENCRYPTION_KEY}

# Network Configuration
DOCKER_NETWORK=${PLATFORM_NAME}_network
DOCKER_SUBNET=172.20.0.0/16
DOCKER_GATEWAY=172.20.0.1

#------------------------------------------------------------------------------
# PORT ALLOCATION SYSTEM
# Base Port: 11000 + (PLATFORM_SN * 200)
# Platform Port Range: BASE_PLATFORM_PORT to BASE_PLATFORM_PORT + 199
#
# Platform SSH: Platform base port (1 per platform)
# Project Port Range: BASE_PROJECT_PORT to BASE_PROJECT_PORT + 19
#
# Port Structure per Project (20 ports):
#   PRODUCTION (0-9):  BE-Node, BE-Python, API-GraphQL, API-REST, API-Reserved, FE-Next, FE-Svelte, FE-Reserved, SYS-Reserved, SYS-Reserved2
#   DEVELOPMENT (10-19): Same as PROD but +10 offset
#------------------------------------------------------------------------------


#------------------------------------------------------------------------------
## PLATFORM PORTS
### Platform Port
PLATFORM_PORT_START=${BASE_PLATFORM_PORT}
PLATFORM_PORT_END=${PLATFORM_PORT_END}

# SSH (Platform-level, single port per platform)
PLATFORM_SSH_PORT=${BASE_PLATFORM_PORT}
PLATFORM_SSH_ROOT_PASSWORD=Answjdtka1!

#------------------------------------------------------------------------------
## PROJECT PORTS


#------------------------------------------------------------------------------
# PATH
## NAS
NAS_ROOT_LOCAL=/volume1
NAS_ROOT_DOCKER=/nas

## Current Directory
CURRENT_DIR_LOCAL=./
CURRENT_DIR_DOCKER=/exposed

## bashrc, crontab
BASHRC_LOCAL=./configs/.bashrc
CRONTAB_LOCAL=./configs/crontab
BASHRC_DOCKER=/root/.bashrc
CRONTAB_DOCKER=/etc/cron.d/app-crontab

## Global Installed Node Modules
GOLBAL_NODE_MODULES_LOCAL=./configs/node_modules
GOLBAL_NODE_MODULES_DOCKER=/root/.nvm/versions/node/v22.19.0/lib/node_modules

## IDE Server Paths (for VSCode and Cursor persistence)
NAS_CURSOR_SERVER_LOCAL=./configs/ide/.cursor-server
NAS_VSCODE_SERVER_LOCAL=./configs/ide/.vscode-server
NAS_CURSOR_SERVER_DOCKER=/root/.cursor-server
NAS_VSCODE_SERVER_DOCKER=/root/.vscode-server

## .claude
CLAUDE_SETTING_LOCAL=./claude
CLAUDE_SETTING_DOCKER=/root/.claude

## CHROME
### 실행 파일 경로 (Docker global installation - Playwright 1.40.0)
CHROMIUM_EXECUTABLE_PATH=/ms-playwright/chromium-1109/chrome-linux/chrome